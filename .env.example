# --- LLMod.ai (OpenAI-compatible) -------------------------------------------
# Create a NEW project key on the LLMod.ai platform for THIS project.
# Do NOT reuse the RAG assignment key. Budget: $9 total, shared by the group.
LLMOD_API_KEY=sk-your-new-llmod-project-key

# OpenAI-compatible base URL for LLMod.ai (reuse the value from the platform).
LLMOD_BASE_URL=https://api.llmod.ai/v1

# Chat model id to use (pick the cheapest model that meets quality — protects the $9 budget).
LLMOD_MODEL=gpt-4o-mini
